const User = require('../models/user');


/**
 * @description API end point to sign up a user 
 * @method POST
 * @param {*} req 
 * @param {*} res 
 */
exports.signUpUser = async (req, res) => {
    try { 

    } catch (error) {

     }
}
